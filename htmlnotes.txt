26/11/24
*HTML:-----
--it is use to create stucture of web app.
--it is use to display data to user on browser.
--it stand for hyber text markup languages.
--the latest version of html is html5.

-it is tag basis languages
<tag>
-in html we have two type tag.
1.paired tag:<starting tag></ending tag>
2.unpaired tag:<starting tag>

*step to create html file------
step1:cretae html file filename.html or filename.htm
step2:write a code in it
step3: execute the browser


Doctype html:it tell serach engine this is html type of document belong to version 


head:it contain external link and meta data in it.
body:write a code to display on browser.

meta data: meta tag gives us information about data.

task;------
(a+b)^2=a^2+b^2+2ab
h2so4
in html <br> tag is used for line

*element : it contain <starting tag attribute="value"> content </ending tag>
attribute:it provide additional information to element
        --it always weite in startingtag.
        --it comes with key and values pairs formate.

*in html we have two types of element:---
1.block element:the element which takes 100% of width
                -it always start on new line
                -ex:h1-h6,p,li,table,form
2.inline element: the elementwhich takes required width of it
                -it will never start on new lines
                -ex:b,i,u,img,a

*type of list in html
-three type of list available
1.ordered list(ol):type=>number,A,B,C,a,b,c,I,II,III,i,ii,iii
2.unordered list(ul):type=>disc,circle,square,none
3.description list(dl):there are tag
                        -dl:descriptionlist
                        -dt:define term
                        -dd:define defination.




*insert images by using relative path
*insert multimedia file by using relative path


Q&a
1,what is diff between html and htnl5?
html5 -symantic element introduce:the element which has meaning in it same
                                --ex:header,footer,nav,main,section,aside
2.new input type,new attribute and new tag.
3.multimedia element introduce:we can insert video and audio file.
4.html API introduce:
        --geolocation :-it return user current location
                        -if you want to use it, gps must have install in system
        --Drag and Drop:Grabbing an object from one location and put at another place.
        --web worker :it is small java script code which execute at backend browser without affecting its current execution
        --web storages :it is used to stored user data on browser
                        -there  are two type of webstorages
                        1.local storages:it is use to store user data for permanantly
                        2.session storages:it is use to store user data for limited time
        --SSE(server sent event):it allow us to get automatic update from server

*iframe:it is use to create small window in webpages
-we can use iframe tag for 
1.display youtube video
2.display googlemap location
3.display one website into another website


*link:it is use for navigation
--to create link in html using another tag

*how to create basic website

*table:it is collection if row and colum